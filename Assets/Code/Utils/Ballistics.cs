namespace Utils {

    public static class Ballistics {

        /*public static float[] Angles (XY targetOffset, float v, float g) {
            float dx = targetOffset.X;
            float dy = targetOffset.Y;
            
            float temp = Mathf.Sqrt(Mathf.Pow(v, 4) + 2 * dy * g * v * v - g * g * dx * dx);
            float temp2 = v * v + g * dy;
            
            float t1 = Mathf.Sqrt(2 * (temp2 - temp)) / g;
            float t2 = Mathf.Sqrt(2 * (temp2 + temp)) / g;

            float a1 = Mathf.Acos(dx / (v * t1));
            float a2 = Mathf.Acos(dx / (v * t2));
            // 0 - pi

            if ((dy - g * t1 * t1 / 2) / (t1 * v) < 0) a1 = -a1;
            if ((dy - g * t2 * t2 / 2) / (t2 * v) < 0) a2 = -a2;
        }*/

    }


//    public static float X (XY targetOffset, XY v, float y, float g) {
//    }

}